# Portfolio React

Estrutura base de portfolio em React + Vite com componentes separados por seção.

## Como executar

```bash
npm install
npm run dev
```

## Estrutura

O projeto segue a organização solicitada, com:

- `src/components` para seções da interface.
- `src/pages` para páginas.
- `src/data` para dados estáticos.
- `src/styles` para estilos globais e variáveis.
